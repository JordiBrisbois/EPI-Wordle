# Port du serveur (defaut: 3000)
PORT=3000

# Secret pour signer les tokens JWT (A CHANGER EN PRODUCTION !)
# Generez une longue chaine aleatoire pour la securite
JWT_SECRET=change_this_secret_key_to_something_long_and_random

# Mode environnement (development | production)
NODE_ENV=development
